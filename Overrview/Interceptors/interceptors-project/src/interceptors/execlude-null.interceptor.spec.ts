import { ExecludeNullInterceptor } from './execlude-null.interceptor';

describe('ExecludeNullInterceptor', () => {
  it('should be defined', () => {
    expect(new ExecludeNullInterceptor()).toBeDefined();
  });
});
